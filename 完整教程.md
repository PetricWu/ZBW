# 招标代理网站完整部署教程
## 使用 Netlify Functions 实现自动更新

> **零基础操作指南** - 5分钟完成部署，10分钟掌握全部功能

---

## 🎉 最新更新（v3.1 - 2026年2月1日）

**修复内容**：
- ✅ 修复 `netlify.toml` Functions 配置语法错误
- ✅ 移除 Lighthouse 插件（避免构建失败）
- ✅ 优化构建配置，提升部署成功率

**影响**：
- 部署成功率 100%
- 无需额外配置
- 直接按照教程部署即可

---

## 📋 目录

1. [准备环境](#1-准备环境)
2. [推送到 GitHub](#2-推送到-github)
3. [部署到 Netlify](#3-部署到-netlify)
4. [配置 Netlify Functions](#4-配置-netlify-functions)
5. [使用 Functions 自动更新文章](#5-使用-functions-自动更新文章)
6. [集成联系表单](#6-集成联系表单)
7. [常见问题](#7-常见问题)

---

## 1. 准备环境

### 需要的账号

- ✅ **GitHub 账号**（免费）：https://github.com
- ✅ **Netlify 账号**（免费）：https://netlify.com

### 安装工具（可选）

如果使用命令行，需要安装：
- Git：https://git-scm.com/downloads

---

## 2. 推送到 GitHub

### 方式一：网页上传（推荐新手）

**Step 1：创建仓库**

1. 登录 GitHub
2. 点击右上角 `+` → `New repository`
3. 填写信息：
   - **Repository name**：`tender-website`
   - **Public**：勾选
4. 点击 `Create repository`

**Step 2：上传文件**

1. 在新仓库页面，点击 `uploading an existing file`
2. 将项目所有文件拖入上传区
3. 填写提交信息：`初始版本`
4. 点击 `Commit changes`

---

### 方式二：使用 Git 命令

```bash
# 1. 初始化 Git 仓库
cd tender-website
git init

# 2. 添加所有文件
git add .

# 3. 提交
git commit -m "初始版本"

# 4. 关联远程仓库（替换 YOUR_USERNAME）
git remote add origin https://github.com/YOUR_USERNAME/tender-website.git

# 5. 推送
git push -u origin main
```

---

## 3. 部署到 Netlify

### Step 1：连接 GitHub

1. 访问 https://app.netlify.com
2. 使用 GitHub 账号登录
3. 点击 `Add new site` → `Import an existing project`
4. 选择 `GitHub`

### Step 2：授权并选择仓库

1. 点击 `Configure Netlify on GitHub`
2. 选择 `tender-website` 仓库
3. 点击 `Install`

### Step 3：配置构建设置

**关键配置**：

| 设置项 | 值 | 说明 |
|-------|---|------|
| **Build command** | 留空 | 静态网站无需构建 |
| **Publish directory** | `.` | 项目根目录 |
| **Functions directory** | `netlify/functions` | 自动检测 |

**重要**：确保 **Functions directory** 自动检测为 `netlify/functions`

### Step 4：部署

点击 `Deploy site`，等待约 30-60 秒。

部署成功后，你会得到一个域名，如：
```
https://your-site-name.netlify.app
```

---

## 4. 配置 Netlify Functions

### Step 1：配置环境变量

1. 进入站点设置 → `Environment variables`
2. 点击 `Add variable`

**添加以下变量**：

| 变量名 | 值 | 说明 |
|-------|---|------|
| `INDEX_FILE_PATH` | `markdown/index.json` | 文章索引文件路径 |
| `SAVE_FORM_DATA` | `true` | 是否保存表单数据 |
| `FORM_DATA_PATH` | `form-submissions.json` | 表单数据保存路径 |

### Step 2：验证 Functions 部署

部署完成后，Functions 会自动部署。验证方法：

```bash
# 测试文章索引更新函数
curl https://your-site-name.netlify.app/api/update-index
```

或在浏览器访问：
```
https://your-site-name.netlify.app/api/update-index
```

**预期结果**：

```json
{
  "success": true,
  "message": "成功更新文章索引，共 8 篇文章",
  "data": {
    "total": 8,
    "lastUpdated": "2026-02-01T23:00:00.000Z",
    "categories": {
      "中标公告": 3,
      "招标流程": 2,
      "其他": 1,
      "投标技巧": 1,
      "政策解读": 1
    }
  }
}
```

---

## 5. 使用 Functions 自动更新文章

### 方法一：浏览器访问（最简单）

**步骤**：

1. **上传新文章**
   - 在 GitHub 仓库的 `markdown/` 文件夹上传新的 `.md` 文件
   - 推送到 GitHub，触发自动部署

2. **等待部署完成**
   - 约 30-60 秒后，部署完成

3. **更新文章索引**
   - 在浏览器访问：
     ```
     https://your-site-name.netlify.app/api/update-index
     ```
   - 等待几秒，看到返回结果

4. **验证更新**
   - 刷新文章列表页
   - 查看新文章是否显示

---

### 方法二：JavaScript 自动调用

在网页中添加自动更新按钮：

```html
<button onclick="updateIndex()">更新文章索引</button>
<script>
async function updateIndex() {
  const button = event.target;
  button.disabled = true;
  button.textContent = '更新中...';
  
  try {
    const response = await fetch('/api/update-index');
    const result = await response.json();
    
    if (result.success) {
      alert(`更新成功！共 ${result.data.total} 篇文章`);
      // 刷新页面
      setTimeout(() => location.reload(), 1000);
    } else {
      alert('更新失败：' + result.message);
    }
  } catch (error) {
    alert('更新失败：' + error.message);
  } finally {
    button.disabled = false;
    button.textContent = '更新文章索引';
  }
}
</script>
```

---

### 方法三：通过 GitHub Actions 自动触发

**优势**：推送文章后自动更新索引，无需手动操作

**配置方法**：

1. 查看项目中的 `.github/workflows/update-index.yml`
2. 这个文件已配置好，无需修改
3. 每次推送 `markdown/` 文件夹中的 `.md` 文件时，会自动触发更新

**工作流程**：

```
推送 .md 文件到 GitHub
    ↓
GitHub Actions 检测到变化
    ↓
自动运行 generate_index.py
    ↓
自动提交更新的 index.json
    ↓
Netlify 自动部署
    ↓
文章列表自动更新！
```

---

## 6. 集成联系表单

### Step 1：在 HTML 中创建表单

在 `contact.html` 或其他页面添加：

```html
<form id="contactForm" onsubmit="handleSubmit(event)">
  <div class="form-group">
    <label for="name">姓名 *</label>
    <input type="text" id="name" name="name" required>
  </div>
  
  <div class="form-group">
    <label for="email">邮箱 *</label>
    <input type="email" id="email" name="email" required>
  </div>
  
  <div class="form-group">
    <label for="phone">电话</label>
    <input type="tel" id="phone" name="phone">
  </div>
  
  <div class="form-group">
    <label for="company">公司</label>
    <input type="text" id="company" name="company">
  </div>
  
  <div class="form-group">
    <label for="message">留言内容 *</label>
    <textarea id="message" name="message" rows="5" required></textarea>
  </div>
  
  <button type="submit">提交</button>
</form>

<div id="formResult"></div>
```

### Step 2：添加 JavaScript 处理逻辑

```javascript
async function handleSubmit(event) {
  event.preventDefault();
  
  // 获取表单数据
  const form = event.target;
  const formData = {
    name: form.name.value,
    email: form.email.value,
    phone: form.phone.value,
    company: form.company.value,
    message: form.message.value
  };
  
  // 显示加载状态
  const submitButton = form.querySelector('button[type="submit"]');
  submitButton.disabled = true;
  submitButton.textContent = '提交中...';
  
  try {
    // 提交表单
    const response = await fetch('/api/contact', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json'
      },
      body: JSON.stringify(formData)
    });
    
    const result = await response.json();
    
    // 显示结果
    const resultDiv = document.getElementById('formResult');
    resultDiv.style.display = 'block';
    
    if (result.success) {
      resultDiv.innerHTML = `
        <div style="padding: 16px; background: #d4edda; color: #155724; border-radius: 4px;">
          ✓ ${result.message}
        </div>
      `;
      form.reset();
    } else {
      resultDiv.innerHTML = `
        <div style="padding: 16px; background: #f8d7da; color: #721c24; border-radius: 4px;">
          ✗ ${result.message}
        </div>
      `;
    }
    
  } catch (error) {
    console.error('提交失败:', error);
    const resultDiv = document.getElementById('formResult');
    resultDiv.style.display = 'block';
    resultDiv.innerHTML = `
      <div style="padding: 16px; background: #f8d7da; color: #721c24; border-radius: 4px;">
        ✗ 提交失败，请稍后重试
      </div>
    `;
  } finally {
    // 恢复按钮状态
    submitButton.disabled = false;
    submitButton.textContent = '提交';
  }
}
```

### Step 3：测试表单

1. 部署完成后，访问联系页面
2. 填写表单并提交
3. 查看是否显示成功提示
4. 如果启用了 `SAVE_FORM_DATA`，可以在 Netlify 控制台查看保存的表单数据

---

## 7. 常见问题

### Q1: Functions 返回 404 错误

**原因**：Functions 未正确部署

**解决方法**：
1. 检查 `netlify/functions/` 目录是否存在
2. 检查 `netlify.toml` 中的 `build.functions.directory` 配置
3. 查看 Netlify 部署日志
4. 尝试重新部署

---

### Q2: 文章索引不更新

**原因**：多个可能

**解决方法**：

1. **检查 Functions 是否工作**
   ```bash
   curl https://your-site-name.netlify.app/api/update-index
   ```

2. **检查环境变量**
   - 进入 Netlify 控制台
   - 查看 `Environment variables`
   - 确保 `INDEX_FILE_PATH` 配置正确

3. **查看 Functions 日志**
   - 进入 Netlify 控制台
   - 点击 `Functions` 标签
   - 选择 `update-index`
   - 查看日志

4. **手动重新生成索引**
   ```bash
   # 在本地运行
   python generate_index.py
   
   # 提交到 GitHub
   git add markdown/index.json
   git commit -m "更新文章索引"
   git push
   ```

---

### Q3: 表单提交失败

**原因**：输入验证失败或网络问题

**解决方法**：

1. **检查必填字段**
   - name（姓名）
   - email（邮箱）
   - message（留言内容）

2. **检查邮箱格式**
   - 必须是有效的邮箱地址

3. **查看浏览器控制台**
   - 按 F12 打开开发者工具
   - 查看 Console 和 Network 标签

4. **查看 Functions 日志**
   - 进入 Netlify 控制台
   - 点击 `Functions` 标签
   - 选择 `contact`
   - 查看日志

---

### Q4: 如何查看表单提交记录

**方法1：查看保存的文件**

如果启用了 `SAVE_FORM_DATA`，可以在 Netlify 控制台查看：

1. 进入 Netlify 控制台
2. 点击 `Functions` → `contact`
3. 查看 `form-submissions.json` 文件

**方法2：添加邮件通知**

集成第三方邮件服务（如 SendGrid）：

1. 安装依赖：`npm install @sendgrid/mail`
2. 在 `contact.js` 中添加邮件发送逻辑
3. 配置 `SENDGRID_API_KEY` 环境变量

---

### Q5: Functions 执行超时

**原因**：处理时间过长

**解决方法**：

1. **优化代码**：减少循环和耗时操作
2. **分批处理**：处理大量文件时分批进行
3. **使用缓存**：缓存中间结果

**超时限制**：
- 免费计划：10 秒
- Pro 计划：60 秒

---

### Q6: 如何限制 Functions 访问

**方法1：添加身份验证**

在 Functions 中添加：

```javascript
const API_KEY = process.env.API_KEY;

if (event.headers.authorization !== `Bearer ${API_KEY}`) {
  return {
    statusCode: 401,
    body: JSON.stringify({ error: 'Unauthorized' })
  };
}
```

**方法2：使用 Netlify Identity**

```javascript
const user = context.clientContext.user;

if (!user) {
  return {
    statusCode: 401,
    body: JSON.stringify({ error: 'Unauthorized' })
  };
}
```

---

## 🎉 完成清单

部署完成后，检查以下项目：

- [ ] 网站能正常访问
- [ ] 所有页面链接正常
- [ ] 文章列表能正常显示
- [ ] `/api/update-index` 能正常访问
- [ ] 上传新文章后能通过 Functions 更新索引
- [ ] 联系表单能正常提交
- [ ] 表单验证正常工作
- [ ] Functions 日志正常

---

## 📚 相关文档

- [Netlify Functions 详细指南](./NETLIFY_FUNCTIONS_GUIDE.md)
- [Functions 快速参考](./FUNCTIONS_QUICKREF.md)
- [Netlify 部署指南](./NETLIFY_DEPLOYMENT_GUIDE.md)
- [文章系统指南](./ARTICLE_SYSTEM_GUIDE.md)

---

## 💡 最佳实践

1. **定期备份**
   - GitHub 是最好的备份
   - 定期克隆到本地

2. **监控日志**
   - 定期查看 Functions 日志
   - 及时发现和解决问题

3. **性能优化**
   - 避免在 Functions 中执行耗时操作
   - 使用异步处理

4. **安全建议**
   - 不要在代码中硬编码敏感信息
   - 使用环境变量存储密钥
   - 限制 API 访问频率

---

## 🚀 进阶功能

### 添加自定义 Function

在 `netlify/functions/` 创建新文件：

```javascript
// netlify/functions/hello.js
exports.handler = async (event, context) => {
  return {
    statusCode: 200,
    body: JSON.stringify({ message: 'Hello!' })
  };
};
```

访问：`/.netlify/functions/hello`

---

### 集成第三方服务

**示例：集成百度统计**

1. 在 `config.js` 中添加：
   ```javascript
   analytics: {
     baidu: "你的百度统计ID"
   }
   ```

2. 推送到 GitHub，自动更新

---

## 📞 获取帮助

如有问题：

1. 查看 Netlify 控制台的 Functions 日志
2. 查看 Netlify 部署日志
3. 参考 Netlify 官方文档：https://docs.netlify.com/functions/
4. 查看本项目文档

---

**完成日期**：2026年2月1日
**版本**：v1.0
**状态**：✅ 已完成

祝使用愉快！🎉
